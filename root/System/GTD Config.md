---
vaultMode: canonical
taskMarker: "#task"
inboxTag: "#inbox"
nextTag: "#next"
waitingTag: "#waiting"
rootLabel: gtd
importLabel: gtd/import
waitingLabel: gtd/waiting
reviewLabel: gtd/review
referenceLabel: gtd/reference
importedLabel: gtd/imported
signalCalendar: GTD Signals
signalSyncEnabled: true
peopleLinkingEnabled: true
transcryptEnabled: true
---

# GTD Config

This note captures the local runtime configuration for the opinionated `gws-gtd` workflow.

Defaults intentionally match the canonical package model. Override only when the vault is in retrofit mode and a local exception is genuinely required.
