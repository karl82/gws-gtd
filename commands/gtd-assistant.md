---
description: Invoke the persistent GTD assistant persona with warm-start and coach.
agent: gtd-assistant
subtask: true
---

Start the persistent GTD assistant.

Scope and notes: $ARGUMENTS

Follow `AGENTS.md` and `gws-gtd` in `assistant` mode on first turn (warm-start + menu) and `coach` mode on subsequent turns (ambient nudges per `coach.md` rhythm model).
