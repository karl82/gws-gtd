---
description: Classify, normalize, and clean up notes and tasks per canonical gws-gtd conventions.
agent: gtd-organizing
subtask: true
---

Classify, normalize, and clean up the vault structure per canonical `gws-gtd` conventions.

Scope and notes: $ARGUMENTS

Follow `AGENTS.md` and `gws-gtd` in `organizing` mode. Per-move confirmation required. For migration (legacy Designs/ reconciliation, Google Docs matching), use `/gtd-retrofit` instead.

Output per `organizing.md § Output`.
