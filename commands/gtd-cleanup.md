---
description: Clean up vault structure and archive inactive work
agent: gtd-organizing
subtask: true
---
Run structural cleanup for this repository.

Scope and notes: $ARGUMENTS

Requirements:
- Follow `AGENTS.md` and `gws-gtd` in `organizing` mode.
- Move completed or inactive notes to `Archive/` when appropriate.
- Use minimal edits and provide an explicit move/change list.
- Preserve canonical task syntax and lifecycle folder semantics.
- Run as an interactive facilitation session.
- Ask one focused question at a time and wait for my response.
