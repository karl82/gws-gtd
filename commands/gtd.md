---
description: Run a GTD ceremony or invoke the GTD assistant.
agent: gtd
subtask: true
---

Run GTD for this vault.

Scope and notes: $ARGUMENTS

If `$ARGUMENTS` specifies a ceremony (`daily`, `weekly`, `monthly`, `organize`, `signals`, `sweep`, `drain`), run that. Otherwise enter assistant mode (warm-start + interactive Q&A) per `reference.md § Assistant mode`.

Output decisions, blockers, and unresolved questions after each step. End the ceremony when the user confirms or no more candidates remain.
